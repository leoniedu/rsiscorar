% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-geojson.R
\name{write_uv_geojson}
\alias{write_uv_geojson}
\title{Write Predictions to UV GeoJSON File}
\usage{
write_uv_geojson(
  dt,
  output_file,
  resolution = 0.005,
  hours = 0:23,
  area = NULL
)
}
\arguments{
\item{dt}{data.table: prediction data from \code{\link[=predict_currents]{predict_currents()}} with columns
\code{lat}, \code{lon}, \code{hour}, \code{u_velocity}, \code{v_velocity} (all in cm/s).}

\item{output_file}{Character: output file path (should end in \code{.geojson}).}

\item{resolution}{Numeric: grid resolution in degrees for downsampling.
Default: \code{0.005} (~500 m at Baia de Todos os Santos).}

\item{hours}{Integer vector: which hours (UTC) to include. Default: \code{0:23}.}

\item{area}{Character: area name included in \code{meta}. Default: \code{NULL}
(omitted from meta).}
}
\value{
Invisibly returns the output file path.
}
\description{
Exports tidal current predictions to a compact GeoJSON FeatureCollection
suitable for consumption by web/native clients. Each feature represents one
grid point and carries 24-hour arrays of U, V, speed, and direction.
}
\details{
The function downsamples the prediction grid to \code{resolution} degrees by
rounding coordinates and averaging U and V within each cell per hour.
Speed (\code{s}, m/s) and direction (\code{d}, degrees clockwise from North,
oceanographic convention) are derived from the averaged U/V.

Output GeoJSON structure:

\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{
  "type": "FeatureCollection",
  "meta": \{"date": "2025-02-25", "resolution": 0.005\},
  "hours_utc": [0, 1, ..., 23],
  "features": [\{
    "type": "Feature",
    "geometry": \{"type": "Point", "coordinates": [lon, lat]\},
    "properties": \{
      "u": [...],
      "v": [...],
      "s": [...],
      "d": [...]
    \}
  \}]
\}
}\if{html}{\out{</div>}}

Requires the \code{jsonlite} package.
}
\examples{
\dontrun{
dt <- predict_currents("2025-06-15", "baiatos")
write_uv_geojson(dt, "baiatos_20250615_005.geojson", area = "baiatos")
}

}
\seealso{
\code{\link[=write_grib]{write_grib()}}, \code{\link[=predict_currents]{predict_currents()}}
}
