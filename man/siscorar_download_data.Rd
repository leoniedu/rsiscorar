% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{siscorar_download_data}
\alias{siscorar_download_data}
\title{Download SISCORAR Data Files}
\usage{
siscorar_download_data(
  areas = SISCORAR_AREAS,
  destdir = normalizePath("~/bin/siscorar-5.0", mustWork = FALSE),
  overwrite = FALSE
)
}
\arguments{
\item{areas}{Character vector of bay names to download. Defaults to all
five areas. See \code{\link[=siscorar_areas]{siscorar_areas()}}.}

\item{destdir}{Character: destination directory. Defaults to
\verb{~/bin/siscorar-5.0} (the default \code{\link[=siscorar_home]{siscorar_home()}} path).}

\item{overwrite}{Logical: re-download and overwrite existing files?
Default \code{FALSE} skips areas whose U.bin already exists.}
}
\value{
Invisibly returns the destination directory path.
}
\description{
Downloads harmonic constants (U.bin, V.bin) and supporting files
(ConsNovas.txt, Interp.bin) for the requested bay areas from the official
SISCORAR distribution ZIP hosted by the Brazilian Navy.
}
\details{
This is the recommended first step for new users. Only the files needed
for \code{\link[=predict_currents]{predict_currents()}} are downloaded — the Windows prediction
executables are not fetched.

Downloads from:
\verb{https://www.marinha.mil.br/chm/sites/www.marinha.mil.br.chm/files/siscorar-5.0.zip}

The ZIP is downloaded to a temporary file and deleted after extraction.
Files extracted per area: \code{U.bin}, \code{V.bin}, \code{ConsNovas.txt}, \code{Interp.bin}.
}
\examples{
\dontrun{
# Download all areas (first-time setup)
siscorar_download_data()

# Download specific areas only
siscorar_download_data(areas = c("sepetiba", "guanabara"))

# Download to a custom location
siscorar_download_data(destdir = "/opt/siscorar-5.0")
}

}
\seealso{
\code{\link[=predict_currents]{predict_currents()}}, \code{\link[=siscorar_sitrep]{siscorar_sitrep()}}
}
